[package]
name = "xram-cuda"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Runtime-dlopen binding to the CUDA driver API, so XRAM builds and tests without a GPU present."

[dependencies]
libc.workspace = true
thiserror.workspace = true
